{
  "name": "zenverse",
  "version": "1.0.0",
  "description": "ZenVerse Dating App - Full Stack",
  "main": "server/server.js",
  "scripts": {
    "start": "npm run build && cd server && npm ci --production && node server.js",
    "build": "cd client && npm ci --production && npm run build:prod",
    "dev": "cd server && npm run dev",
    "seed": "cd server && npm run seed"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "socket.io-client": "^4.8.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
