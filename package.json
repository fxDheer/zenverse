{
  "name": "zenverse",
  "version": "1.0.0",
  "description": "ZenVerse Dating App - Full Stack",
  "main": "server/server.js",
  "scripts": {
    "start": "npm run build && cd server && npm ci --production && node server.js",
    "build": "cd client && npm ci --production --no-optional && GENERATE_SOURCEMAP=false CI=false npm run build",
    "build:light": "cd client && npm ci --production --no-optional --no-audit --no-fund && GENERATE_SOURCEMAP=false CI=false SKIP_PREFLIGHT_CHECK=true npm run build",
    "dev": "cd server && npm run dev",
    "seed": "cd server && npm run seed"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "socket.io-client": "^4.8.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
